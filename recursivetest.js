// class PersonNode {
//   constructor(person) {
//     this.value = person.name;
//     this.person = person;
//     this.left = null;
//     this.right = null;
//   }

//   add(node) {
//     if(this.value < node.value) {
//       if(this.left === null) {
//         this.left = node;
//       } else {
        
//       }
//     } 
//   };
// }

// let node;

// node = root.find('Nelson');

// node = root.find('Foo');
